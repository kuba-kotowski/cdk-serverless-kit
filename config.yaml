project_name: CDKServerlessKit # once set it should not be changed, as stack will change and resources will be recreated

region: eu-central-1

env_name: main

api_gateway: true
auth: lambda # options: null, cognito, lambda
secret_key: api_key # optional for lambda authorizer, default is 'api_key'

dynamodb: true